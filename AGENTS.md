# OurBot development instructions

## 安安大冒險更新公告

修改「安安大冒險」時，在完成任務前檢查這次變更是否對玩家可見。

需要更新 `config/update.toml` 的情況：

- 新玩法、新系統、新活動、新道具或新角色。
- 數值平衡、掉落、獎勵、消耗、難度或經濟調整。
- 玩家看得到的指令、介面、頻道、流程或規則變更。
- 會影響遊戲結果或玩家體驗的重要錯誤修正。

不要更新 `config/update.toml` 的情況：

- 純重構、測試、記錄、開發工具或維運調整，且遊戲行為沒有改變。
- AI 聊天、圖片、語音或其他與安安大冒險無關的功能。
- 只有開發者看得到的修正，且不影響玩家。

編輯公告時：

- `version` 必須是新的唯一值，格式使用 `YYYY.MM.DD-N`；當天從 `-1` 開始依序增加。
- 用玩家看得懂的繁體中文撰寫 `title`、`summary` 與 `changes`，不寫內部類別名、檔名或實作細節。
- 如果 `config/update.toml` 已有未提交的其他功能公告，保留它們，把新項目合併到同一版公告，不覆蓋別人的內容。
- 只有文案或文件改變而沒有 RPG 玩家可見變更時，不得為了凑版本而更動公告。
- 任務交付時要提到是否有更新公告，以及判斷理由。
