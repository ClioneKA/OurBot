[tool.poetry]
name = "repl_python3_beta_NewBot"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"
flask = "^1.1.2"
python-dotenv = "^0.14.0"
"discord.py" = ">=2.0.0"
openai = ">=1.109.0"
requests = ">=2.31.0"
Pillow = ">=9.0.0"
PyNaCl = "^1.4.0"
ffmpeg = "^1.4"
tomli = { version = ">=2.0.1,<3", python = "<3.11" }

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
